[project]
name = "foodguardai"
version = "1.0.0"
description = "AI-powered food safety and allergen analysis API"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
]

[tool.fastapi]
entrypoint = "app.main:app"
